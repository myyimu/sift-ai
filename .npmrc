# Public builds use the upstream package registry and Electron release source by
# default. Developers behind a restricted network may set temporary
# npm_config_electron_mirror / npm_config_electron_builder_binaries_mirror
# environment variables locally. Do not commit organization-specific mirrors or
# credentials here.
