# P0.5 实现状态

更新时间：2026-08-31

## 已落地：Stage A（离线内容身份与分级抽取）

- 新增 `@sift/units` workspace 包。
- `UnitExtractor-v1.2` 按 `Semantic → Repeated Structure → Main Content fallback` 顺序运行。
- 输入只接受已捕获、已脱敏的 HTML；不访问 URL、不调用 LLM、不使用 OCR。
- 具备确定性节点/深度/输入字节/候选数量/Unit 数量限制。
- 产出不可变 `UnitObservation`、Observation-owned `EvidenceBlock`、去重 `EvidenceBlob`、`CanonicalUnit` 和 `DerivedMetadata`。
- 身份优先级为 permalink → namespaced anchor → unkeyed；unkeyed 不按相同文本合并。
- `full` Observation 才能物化 `UnitVersion`；`partial/unknown` 不产生版本；内容回退会复用既有版本指纹。
- 桌面端新增 `extractUnitsForScope` 读侧入口，读取现有 store 快照并聚合跨快照版本。
- `qa-cli` 新增 `--extract-units`，可离线导出 JSON，不需要模型配置。

## 已落地：Stage B1（Ledger 纯函数层）

- 新增幂等 `upsertUnitMaterialization`：重复 Snapshot 不新增 Observation，重复 source link 不重复写入，事实改写直接冲突失败。
- 新增 `resolveCanonicalId` / `mergeCanonicalUnits`：合并方向固定为较早对象，路径解析防环且操作幂等。
- 新增 `deleteUnitObservations`：先移除 Observation-owned occurrence，再按可达性回收 EvidenceBlob、UnitVersion 与 CanonicalUnit。
- Ledger 层保持纯函数，不直接写 Capture Store，避免破坏当前 host 单写者和 Raw Capture 热路径。
- 新增 `unitLedgerPath/readUnitLedger/writeUnitLedger` 派生文件边界，采用 schemaVersion + 临时文件 + 原子 rename；损坏文件 fail-closed。

## 已落地：Stage B2（TopicProjection 纯本地安全层）

- 新增 `@sift/topics` 包，冻结 TopicProjection v1 的结构、范围和引用校验。
- 对模型候选结果执行 fail-closed 校验：所有 Unit/Evidence 引用必须存在、属于当前 scope，且 EvidenceBlock 必须归属于对应 Unit。
- 提供去重后的 Unit/Page/Domain 统计、开方有界节点大小、CanonicalUnit Jaccard 关系权重和确定性网格布局。
- 该层不自动触发模型、不做后台聚类、不显示趋势，也不把 Page 或词频当作主题计数。

## 已落地：Stage C0（按需主题生成最小闭环）

- `ModelAdapter.completeJson` 支持严格 JSON Schema、`json_object` 降级、两次调用上限和本地校验回调。
- 桌面端 `generateTopicProjection` 仅在显式 IPC 调用时运行；先冻结 Unit/Evidence 范围并检查 Unit/Page/Domain/Token/字节上限。
- 主题 UI 采用“本地预览 → 用户确认 → 单次远程生成”两步流程，未确认时模型调用为零。
- TopicProjection 使用内容寻址缓存；相同输入直接本地命中，不重复请求模型。
- 桌面面板新增“最近 7 天 / 30 天生成主题”入口，主题文本与证据 ID 全部使用安全文本节点渲染。

## 已落地：Stage C1（来源与生命周期最小闭环）

- TopicDetail 读取真实 EvidenceBlock / CanonicalUnit / SourceMetadata，并在桌面端展示来源片段与 extent。
- 删除 Session/Page/全部数据时清理 TopicProjection 缓存，避免继续显示已删除证据。
- Ledger 派生文件具备 schemaVersion、原子写入和损坏 fail-closed 读取。

## 已落地：Stage C2（派生一致性）

- Capture host 在非幂等 `commit_ack` 后写入不含正文的 Topic cache stale marker；主题生成发现 marker 时不会复用旧缓存，成功生成后清除 marker。
- 桌面端启动后异步从 Capture Journal 重建全局 Unit Ledger，并通过临时文件 + 原子 rename 完成 checkpoint/compaction；同时按 Session 持久化独立 Ledger，形成 `derived/sessions/<sessionId>/unit-ledger-v1.json` 与全局 Ledger 两层派生视图；重建失败不覆盖旧派生文件。
- 删除 Session/Page/全部数据后同步重建 Ledger，并在成功重建后清理已不存在 Session 的派生文件，避免删除后的旧证据残留在派生层。
- 同一 Session/Page 内稳定内容与 capture extent 重访时复用不可变 UnitObservation，仅追加 source link，避免重复捕获放大 Topic unitCount。
- 相同 payloadHash 的重复快照只解析一次，但保留每个捕获事件的 source link，确保来源可追溯且不重复计算内容。
- 新增 stale marker、Ledger 重建和完整链路回归测试。

## 已落地：Stage C3（问题入口最小闭环）

- 新增版本化 `prompt-pool-v1`（6 个预设问题），仅按当前 scope 的页数选择，不读取正文、不调用实时 LLM。
- 预设问题只填充自由输入框；用户仍可提交任意自主问题，预设项不是白名单或权限边界。
- renderer 通过浏览器安全子入口加载 Prompt Pool，不把 Node 文件系统能力打入前端 bundle。
- 主题结果升级为确定性 Topic Cloud：节点大小按去重 Unit 数，连线按 Jaccard overlap，节点点击进入真实来源详情；同时保留文本说明和键盘可操作入口。
- Topic Cloud 节点使用后端按当前 Ledger 计算的 `unitCount/pageCount/domainCount`，renderer 不再近似计算统计值。
- 捕获更新后的 stale 状态会在桌面面板提示“主题图可能过期”，引导用户重新预览和确认，不静默展示旧结论。
- Topic Detail 打开前重新校验当前 scope 与 Unit/Evidence 引用；过期、跨范围或篡改投影 fail-closed。
- TopicProjection 缓存读取校验 schema、输入摘要、预算摘要和完整引用；损坏或旧版本缓存自动删除并重新生成。
- Topic Cloud 的 `pageCount/domainCount` 现在严格按投影自身时间范围过滤，不会把范围外的历史 Observation 算入当前主题统计。
- 主题范围筛选改用绝对时间戳比较，带不同时区偏移的 ISO 时间不会因字符串排序而漏掉有效 Observation。
- Ledger、Topic cache 与 stale marker 的原子写入临时文件改用 PID + UUID，避免并发维护或异常残留造成临时文件名冲突。
- TopicProjection 缓存键和校验绑定当前 `TOPIC_PROJECTION_VERSION` / `TOPIC_PROMPT_VERSION`，升级归纳契约后不会误复用旧主题结果。

## 部分落地：Stage C4（离线正文回退）

- fallback 现已优先接入 Mozilla Readability `0.6.0`（MPL-2.0），仅对已捕获 HTML 的
  linkedom 独立副本运行，显式关闭 JSON-LD，设置元素上限，不访问 URL、不加载资源；解析
  失败或正文不足时回退到版本化的 `main/article/#content/.post` 根选择。
- 两条回退路径都执行正文门槛和常见侧栏规避：只有标题或空壳页面返回 `extraction_empty`，
  不会伪造 Unit。版本已提升为 `unit-extractor-v1.2`，避免误复用旧派生结果。
- 桌宠入口与完整按需分析 UI 仍未落地；现有主题地图 UI 已足够支持 P0.5 最小演示闭环，
  详见 `RUNBOOK.md` §5.7。

仍未实现后台自动主题分析、趋势/热点、Embedding/RAG、桌宠轮播或浏览器自动化。

因此当前状态是“P0 已可演示，P0.5 数据层与按需主题生成最小闭环已具备”，还不能宣称完整 P0.5 产品形态已完成。
